export default function createClient(host: string, port: number) {
    /**
     * Create a Fastify client instance on the specified host and port.
     * 
     * @param {string} host - Host address of the server.
     * @param {number} port - Port that the server listens on.
     */
   console.log('Placeholder.')
}